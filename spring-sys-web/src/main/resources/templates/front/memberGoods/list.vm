#@html()
    #@wrapper()
        #@panel()
            #@panelHeader({"title":"我的金鸡", "perm": "", "isBack": false, "href": ""})
            #end
        #end
        #@panel()
            #@panelBody()
            <div class="form-group">
                <label class="col-sm-2 control-label">金鸡总数：</label>
                <div class="col-sm-3" style="margin-top: 5px">
                    $!{total.amount}
                </div>
                <label class="col-sm-2 control-label">已下蛋数：</label>
                <div class="col-sm-3">
                    $!{total.addCount}
                </div>
            </div>
            #end
        #end
        #@panel()
             #@panelBody()
                #@table()
                    #tableTHead(["id", "会员编号", "金鸡数量", "每只金鸡每天下金蛋数量", "已下金蛋数量", "创建时间", "到期时间", "金鸡来源"],"", false)
                    #@tableBody($page.list)

                        #tableTd("$!{l.id}")
                        #tableTd("$!{l.memberId}")
                        #tableTd("$!{l.amount}")
                        #tableTd("$!{l.addScoreByonegoods}")
                        #tableTd("$!{l.addCount}")
                        #tableTd($!DateTool.format("yyyy-MM-dd HH:mm:ss", $!{l.createTime}))
                        #tableTd($!DateTool.format("yyyy-MM-dd HH:mm:ss", $!{l.invalidTime}))
                        #tableTd("$!{l.type}", {"type": "dict", "format": "memberGoodsType"})
                        #tableTd("$!{l.state}", {"type": "dict", "format": "goodsState"})

                    #end
                #end

                #parse("modular/page.vm")
            #end
        #end
    #end
    #@script()
    <script>
        $(function () {
            $("#review_id").on('click', function () {
                var arr = "";
                $("[pId='all']:checked").each(function () {
                    if (arr == ""){
                        arr = $(this).val();
                    } else {
                        arr += "," + $(this).val();
                    }
                })
                alert(arr)
                window.location.href = "$!{ctx}/member/delete?id=" + arr;
            })

        })

        $(function () {
            var startTime = $('.strat-time').datepicker().on('changeDate', function(ev) {
                startTime.hide();
            }).data('datepicker');
            var endTime = $('.end-time').datepicker().on('changeDate', function(ev) {
                endTime.hide();
            }).data('datepicker');

            $('button[empty="true"]').on('click', function () {
                $("input[name='startTime']").eq(0).val($('#s_t_h').val());
                $("input[name='endTime']").eq(0).val($('#e_t_h').val());
            })
        })

        /*$.validator.setDefaults({
            submitHandler: function(form) {
                if($('#start_time').val()!=""&&$('#end_time').val()!=""){
                    if (!(new Date($('#start_time').val()).compare(new Date($('#end_time').val()), DATE_CONS.DATE_COMPARE_SMALL_EQUAL))){
                        $("#goods_prompt").html(promptInfo.prompt("结束日期不能小于开始日期"));
                        shade.hideBg("dialog");
                        return;
                    }
                }
                form.submit();
            }
        });*/
        $(document).ready(function () {
            debugger;
            $("#search").validate({
                errorPlacement: function(error, element) {
                    debugger;
                    if (element.is(":checkbox")||element.is(":radio")||element.is(".date-time")){
                        debugger;
                        error.appendTo(element.parent().parent().parent());
                    } else {
                        debugger;
                        error.insertAfter(element.parent());
                    }
                },
                onfocus: true
            });
        })
    </script>
        #jsCalculate()
        #@jsValidate("form_insert")

        #end
    #end
#end