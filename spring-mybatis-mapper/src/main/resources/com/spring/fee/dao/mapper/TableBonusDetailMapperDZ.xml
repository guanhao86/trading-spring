<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spring.fee.dao.mapper.TableBonusDetailMapperDZ" >
  <resultMap id="BaseResultMap" type="com.spring.fee.model.TableBonusDetail" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="member_id" property="memberId" jdbcType="VARCHAR" />
    <result column="bonus_id" property="bonusId" jdbcType="INTEGER" />
    <result column="bonus_type" property="bonusType" jdbcType="INTEGER" />
    <result column="bonus" property="bonus" jdbcType="REAL" />
    <result column="recode_time" property="recodeTime" jdbcType="TIMESTAMP" />
    <result column="remark" property="remark" jdbcType="VARCHAR" />
    <result column="close_state" property="closeState" jdbcType="INTEGER" />
  </resultMap>
  <select id="selectByGroupBonusId" resultMap="BaseResultMap" >
    SELECT bonus_id, sum(bonus) as bonus
    FROM table_bonus_detail
    where 1=1
    <if test="memberId != null and memberId !=''" >
      and member_id = #{memberId,jdbcType=VARCHAR}
    </if>
      <if test="start != null" >
    and CLOSE_Time >= #{start,jdbcType=TIMESTAMP}
      </if>
      <if test="end != null" >
    and #{end,jdbcType=TIMESTAMP} >= CLOSE_Time
      </if>
    group by bonus_id
  </select>
  <select id="selectByGroup" resultMap="BaseResultMap" >
    SELECT bonus_id, sum(bonus) as bonus
    FROM table_bonus_detail
    where 1=1
    and close_state = '1'
    <if test="start != null" >
      and CLOSE_Time >= #{start,jdbcType=TIMESTAMP}
    </if>
    <if test="end != null" >
      and #{end,jdbcType=TIMESTAMP} >= CLOSE_Time
    </if>
    and bonus_id in
    <foreach collection="bonusIdIn" item="id" index="index"
             open="(" close=")" separator=",">
      #{id}
    </foreach>
  </select>
</mapper>