<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spring.fee.dao.mapper.TableBalanceDetailMapperDZ" >
  <resultMap id="BaseResultMap" type="com.spring.fee.model.TableBalanceDetail" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="admin_id" property="adminId" jdbcType="VARCHAR" />
    <result column="last_time" property="lastTime" jdbcType="TIMESTAMP" />
    <result column="bonus_reference" property="bonusReference" jdbcType="REAL" />
    <result column="bonus_group" property="bonusGroup" jdbcType="REAL" />
    <result column="bonus_bole" property="bonusBole" jdbcType="REAL" />
    <result column="bonus_repurchase" property="bonusRepurchase" jdbcType="REAL" />
    <result column="bonus_rank" property="bonusRank" jdbcType="REAL" />
    <result column="operate_time" property="operateTime" jdbcType="TIMESTAMP" />
  </resultMap>
  <select id="selectByGroup" resultMap="BaseResultMap" >
    SELECT sum(bonus_reference) as bonus_reference,
    sum(bonus_group) as bonus_group,
    sum(bonus_bole) as bonus_bole,
    sum(bonus_repurchase) as bonus_repurchase,
    sum(bonus_rank) as bonus_rank
    FROM table_balance_detail
    where 1=1
    <if test="memberId != null" >
      and member_id = #{memberId,jdbcType=VARCHAR}
    </if>
    <if test="start != null" >
    and last_time >= #{start,jdbcType=TIMESTAMP}
    </if>
    <if test="end != null" >
    and #{end,jdbcType=TIMESTAMP} > last_time
    </if>
  </select>
</mapper>